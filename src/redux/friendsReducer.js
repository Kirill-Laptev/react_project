

let initialState = {
    friendsInfo: [
        { id: "1", name: "Sveta" },
        { id: "2", name: "Nikita" },
        { id: "3", name: "Dmitry" },
      ],
}

const friendsReducer = (state = initialState) =>{
    return state;
}

export default friendsReducer;